# PangaeaX Admin Panel

# Initial to Install the Angular Cli 
    1. npm install -g @angular/cli

# To create a new Angular project on a development
    2. ng new pangaeax-admin-panel

# To run a  Angular project on a development    
    3. cd pangaeax-admin-panel

    4. ng serve

# If git clone pangaeax-admin-panel Angular Project install to node module

    1. npm install 

    2. ng serve